{
  "name": "tadarab-ai-worker",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker that verifies a Firebase ID token and proxies quiz/flashcard generation requests to the Claude API.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "jose": "^5.9.6"
  }
}
